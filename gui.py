# import packages
import socket
from Tkinter import *

mainWindow = Tk()

server_socket = socket.socket()
server_socket.connect(('0.0.0.0', 8001))

connection = client_socket.makefile('wb')

Label(image = wb)

mainWindow.mainloop()
